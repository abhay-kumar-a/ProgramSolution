package dsabootcamp.opps.interfaces.extendDemo1;

public interface Man extends Water {  // where we want to inherit onw=e interface into another use extend keyword
                                     // and when inherit interface to class use implements keyword
    void newMan();
}
